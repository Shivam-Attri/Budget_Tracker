# .env.example
# These secrets will be loaded by docker-compose and passed into the container.
# For local development without Docker, `godotenv` will load this file if renamed to .env.
JWT_SECRET="a-very-long-and-random-secret-for-jwt"
ENCRYPTION_KEY="your-super-secret-32-byte-key!!"
